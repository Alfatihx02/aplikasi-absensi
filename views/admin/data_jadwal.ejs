<%- include('../partials/head') %>
<div class="container-scroller">
    <!-- partial:../../partials/_navbar.html -->
    <%- include('../partials/admin-navbar') %>
      <!-- partial -->
      <div class="main-panel">
        <%- include('../partials/messages') %>
        <div class="content-wrapper">
          <div class="row">
            
            <div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">Tabel Jadwal</h4>
                  <a href="/admin/jadwal_create" class="btn btn-rounded btn-primary">Tambah Jadwal</a>
                  <div class="table-responsive">
                    <table class="table table-hover">
                      <thead>
                        <tr>
                          <th>#</th>
                          <th>Hari</th>
                          <th>Waktu Mulai</th>
                          <th>Waktu Selesai</th>
                          <th>Matakuliah</th>
                          <th>Kelas</th>
                          <th>Dosen</th>
                          <th>Ruangan</th>
                          <th>Aksi</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% for( let i = 0; i < rows.length; i++ ) { %>
                        
                            <tr>
                              <td><%= i+1 %></td>
                              <td><%= rows[i].hari %></td>
                              <td><%= rows[i].waktu_mulai %></td>
                              <td><%= rows[i].waktu_selesai %></td>
                              <td><%= rows[i].nama_matakuliah%></td>
                              <td><%= rows[i].nama_kelas %></td>
                              <td><%= rows[i].Nama %></td>
                              <td><%= rows[i].nama_ruangan %></td>
                              <td>
                                <a href="/admin/jadwal_edit/<%= rows[i].id_jadwal %>" class="btn btn-rounded btn-warning">Edit</a>
                                <a href="/admin/jadwal_delete/<%= rows[i].id_jadwal %>" class="btn btn-rounded btn-danger">Hapus</a>
                              </td>
                            </tr>
                        <% } %>
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
        <!-- content-wrapper ends -->
        <!-- partial:../../partials/_footer.html -->

        <!-- partial -->
      </div>
      <!-- main-panel ends -->
    </div>
    <!-- page-body-wrapper ends -->
  </div>
<%- include('../partials/foot') %>